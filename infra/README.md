# Infrastructure

Place Infrastructure as Code and deployment definitions here.

Suggested layout:
- `terraform/`: environment stacks and reusable modules
- `kubernetes/`: manifests or Helm charts (optional)
