# Tool / App Checks

Use this if a tool or app repo needs a few repeatable checks.

## Typical Commands

- install dependencies
- run tests
- verify the build if there is one

## Example Commands

```sh
npm ci
npm test
npm run build
```

If simple setup scripts are enough, use those instead of adding CI.
